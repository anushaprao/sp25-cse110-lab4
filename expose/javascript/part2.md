1. At line 12, 3 will be logged to the console.
2. At line 13, 150 will be logged to the console.
3. At line 14, 150 will be logged to the console.
4. The function returns [50,100,150]. The function takes the input 100, multiplies it by 0.5, multiplies it by 50 * 100, and then divides it by 100, and then rounds, which results in 50. This repeats for 200, and 300, and there are no errors thus far.
5. Since let is being used, i is only defined within the for block and can not be accessed at the console log it is called at.
6. Since let is being used, discountedPrice is only defined within the for block and can not be accessed at the console log it is called at.
7. Line 14 will print the final value of finalPrice which is 150. It will not return an error, even though it is defined by let, because let is defined within the same block that console log is called.
8. The function returns [50,100,150]. The function takes the input 100, multiplies it by 0.5, multiplies it by 50 * 100, and then divides it by 100, and then rounds, which results in 50. This repeats for 200, and 300, and there are no errors thus far.
9. Since let is being used, i is only defined within the for block and can not be accessed at the console log it is called at.
10. Line 12 will return 3, which is the length of the prices array.
11. The function returns [50,100,150]. The function takes the input 100, multiplies it by 0.5, multiplies it by 50 * 100, and then divides it by 100, and then rounds, which results in 50. This repeats for 200, and 300, and there are no errors thus far.
12. A. student.name
    B. student['Grad Year']
    C. student.greeting()
    D. student['Favorite Teacher'].name
    E. student.courseLoad[0]
13. A. '32'
    B. 1
    C. 3
    D. '3null'
    E. 4
    F. 0
    G. '3undefined'
    H. NaN
14. A. true
    B. true
    C. true
    D. false
    E. true
    F. true
15. The == operator does type-conversion before equating, while the === is a strict equality operator which compares value and type.
16. In file part2-question16
17. The result is [2,4,6]. The function modifyArray takes in two parameters: an array and a function. Then a new empty array called newArr is created. For each item in the array, the function passed as the parameter is applied to each element in the array. In this instance, it multiplies each element by 2. This is then pushed into the new empty array, which is then returned.
18. In file part2-question18.js
19. The output is: 1, 4, 3, 2