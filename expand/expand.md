1. The asynchronous nature of javascript refers to it being single-threaded, which means that it can only do one thing at a time. Managing execution order, as well as variable accessability through blocking can be a difficulty. Loose typing most likely refers to the need to be careful about types, especially when doing arithmetic procedures (ex. '2' + '3' = '23, not 5). Javascript has some web platform limitations, such as limited API use, most likely stemming from security concerns.
2. I think that loose typing allows for fast responses, without the need for constant type awareness, and with greater emphasis on quicker output. This could be true because JavaScript is primarily used for websites, which means that it is important to provide fast user responses. I think this may also be why it is asynchronous, because individual lines can run and provide user response/output. Whether they are right or function within the block may remain to be seen, but something is provided as an end result.
3. A compiled language is translated to machine code and then run, while an interpreted language is run in its original language at runtime. As such, compiled languages are usually faster. Javascript is an interpreted language, which allows for faster development and runtime. The drawback is a slower compile time, as well as errors caught at runtime.
4. I think understanding vanilla JS first makes debugging your code easier, because there is a clear understanding of the aforementioned concerns, such as loose typing and asynchronous issues. This gives programmers a clear starting point for debugging. The drawbacks of not learning a framework are that it is difficult to build fully fleshed out and scaled websites/apps without using a framework. Another hypothetical drawback is missing out on "industry buzzword" practices by not learning about frameworks.
5. I think that this lab relates to the project because it is teaching us how JavaScript variables are executed, how to debug using inspector, and the limitations and capabilities of it. This is especially useful as we begin to brainstorm and plan out our own projects and timelines.